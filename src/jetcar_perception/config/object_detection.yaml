object_detection_node:
  ros__parameters:
    engine_path: models/yolov8n.engine
    target_classes:
      - person
      - car
      - stop sign
    confidence_threshold: 0.4
    publish_rate_hz: 10.0
