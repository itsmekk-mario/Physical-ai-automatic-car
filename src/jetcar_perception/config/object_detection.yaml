object_detection_node:
  ros__parameters:
    engine_path: auto
    image_topic: /sensors/stereo/left/image_raw
    target_classes:
      - person
      - car
      - stop sign
    confidence_threshold: 0.4
    max_frame_age_sec: 0.5
    publish_rate_hz: 10.0
